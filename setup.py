from setuptools import setup

setup(name="pipeline",
      version="1.0.0",
      description="apply sequence of functions to iterable elements",
      author="Richard Gerry",
      packages=["pipeline"],
      python_requires=">=3.5")
